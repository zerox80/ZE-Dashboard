# ==========================================
# ZE-Dashboard Configuration
# ==========================================

# --- ENVIRONMENT MODE ---
# Set to "true" for production deployment (enables secure cookies, disables debug)
# Set to "false" for local development
PRODUCTION=false

# --- SECURITY (CRITICAL) ---
# Secret key used for signing JWT tokens and other security cryptographic operations.
# You can generate a secure key using: `openssl rand -hex 32`
SECRET_KEY=change_this_to_a_very_long_random_string_for_production

# Admin password for initial setup. If not set, a random password will be generated.
# IMPORTANT: Set this in production!
ADMIN_PASSWORD=change_this_to_a_secure_password

# --- DATABASE ---
# Connection string for the database. 
# Default is a local SQLite file mounted in the container.
# Format: sqlite:///./<filename.db>
DATABASE_URL=sqlite:///./ze_dashboard.db

# --- BACKEND SETTINGS ---
# Token expiration time in minutes.
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --- RATE LIMITING ---
# Maximum login attempts per minute per IP address
RATE_LIMIT_LOGIN=5/minute

# --- DEBUG MODE ---
# Set to "true" to enable SQL query logging (NEVER enable in production!)
DEBUG_MODE=false

# --- FRONTEND SETTINGS (Optional) ---
# If running independently or on a different domain/port.
# If using the provided Nginx setup, the default relative '/api' path is sufficient.
# VITE_API_URL=http://localhost:8000
